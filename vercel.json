{
    "version": 2,
    "routes": [
      { "src": "/api/v1/(.*)", "dest": "/server.js" },
      { "src": "/api/(.*)", "dest": "/server.js" },
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }